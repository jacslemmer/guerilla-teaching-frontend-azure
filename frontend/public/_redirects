# CloudFlare Pages redirects and rewrites

# API proxy - redirect all /api/* requests to CloudFlare Workers
/api/* https://guerilla-teaching-website.jacobuslemmer.workers.dev/api/:splat 200

# SPA fallback - serve index.html for all non-API routes
/* /index.html 200